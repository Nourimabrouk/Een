# Een Unity Mathematics - Fixed Requirements
# Compatible with Python 3.11+ and avoids platform issues
# ============================================

# Core Scientific Computing (stable versions)
numpy==1.24.3
scipy==1.10.1
pandas==2.0.3
matplotlib==3.7.2
seaborn==0.12.2

# Web Framework
flask==2.3.3
flask-cors==4.0.0
werkzeug==2.3.7

# Visualization
plotly==5.17.0
pillow==10.0.1

# Utilities
tqdm==4.66.1
rich==13.6.0
click==8.1.7
pyyaml==6.0.1
python-dotenv==1.0.0

# Development Tools
pytest==7.4.3
pytest-cov==4.1.0
black==23.11.0
flake8==6.1.0

# Jupyter (optional)
jupyter==1.0.0
notebook==7.0.6
ipykernel==6.27.1

# Additional packages for Unity Mathematics
sympy==1.12
networkx==3.2.1
scikit-learn==1.3.2

# Avoid problematic packages that cause platform issues:
# - torch (can cause platform issues on Windows)
# - tensorflow (heavy and platform-dependent)
# - mayavi (requires VTK which can be problematic)
# - opencv-python (can have platform-specific issues)
# - bitsandbytes (CUDA-specific)
# - flash-attention (CUDA-specific) 