# Streamlit Cloud deployment requirements (Optimized for inotify limits)
streamlit>=1.37.0
numpy>=1.26.4
pandas>=2.2.0
plotly>=5.22.0
scipy>=1.13.0
sympy>=1.12
cachetools>=5.3.0

# Essential for Unity Mathematics
matplotlib>=3.8.0
networkx>=3.2.0

# Removed optional heavy dependencies to reduce file watching overhead
# numba, scikit-learn, seaborn can be imported conditionally if needed